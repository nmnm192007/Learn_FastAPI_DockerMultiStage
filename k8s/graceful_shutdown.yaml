apiVersion: apps/v1
kind: Deployment
metadata:
  name: graceful-shutdown-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: graceful-shutdown-api
  template:
    metadata:
      labels:
        app: graceful-shutdown-api

    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: api
          image: api_with_graceful_shutdown:v1
          ports:
            - containerPort: 8000
          lifecycle:
            preStop:
              exec:
                command: ["sh", "-c", "echo 'PreStop started, PreStop hook running'; sleep 5"]


      
